<div id="wrapper">

    <h1>Saved Articles</h1>
    <button><a href="/" id="viewArticles">View Scraped Articles</a></button>
    </br>
    </br>
    <div id="articles">
        {{#each article}}
        <div id="container" class="article-content" data-id="{{this._id}}">
            <div id="title">
                <strong>{{this.title}}</strong>
            </div>
            <div id="summary">
                {{this.summary}}
            </div>
            <div id="link">
                <a target='_blank' href="{{this.link}}">Get the recipe here</a>
            </div>

            {{#each notes}}
            <div id="notes" class="note-content" data-id="{{this._id}}">
                <h4>Notes: {{this.body}}</h4>
            </div>
            {{/each}}

            <button class="makeNote" data-id="{{this._id}}">Note</button>
            <button class="deleteArticle" data-id="{{this._id}}">Delete</button>
        </div>
        </br>
        {{/each}}
    </div>
    <div id="notes">
        <div class="modal">
            <div class="modal-content"></div>
            <button class="modal-close is-small" aria-label="close"></button>
        </div>
    </div>
</div>
